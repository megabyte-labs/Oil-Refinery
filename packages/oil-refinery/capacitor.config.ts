import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'oilRefinery',
  webDir: '../../dist/packages/oil-refinery',
  bundledWebRuntime: false,
};

export default config;
